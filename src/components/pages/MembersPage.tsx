import { Css } from 'fluxio';
import { Page } from './base/Page';

const c = Css('MembersPage', {
  '': {
  },
});

export const MembersPage = () => {
  return (
    <Page title="" {...c('')}>
    </Page>
  );
};
