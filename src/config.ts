// export const host = 'a.m4k.fr';
// export const appUrl = `https://${host}`;
// export const apiUrl = `${appUrl}/api`;
// export const mediaUrl = `${appUrl}/medias`;
// export const buildUrl = `${appUrl}/build.json`;
// export const appName = host.split('.').reverse()[1];

// const config = {
//   host,
//   appUrl,
//   apiUrl,
//   mediaUrl,
//   appName,
//   buildUrl,
// };

// console.log('config', config);

// export type Config = typeof config;

// export default config;
